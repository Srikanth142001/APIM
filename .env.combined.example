# ═══════════════════════════════════════════════════════════════════════════════
# NexGen APIM — Combined Container Environment Variables
# Copy this file to .env.combined and fill in your values
# ═══════════════════════════════════════════════════════════════════════════════

# ── Port Configuration ─────────────────────────────────────────────────────────
APIM_PORT=8082

# ── Region Name ────────────────────────────────────────────────────────────────
REGION_NAME=SAN Region

# ── Branding & Customization (Optional) ───────────────────────────────────────
# Customize the application name, logo, and UI behavior at runtime
# These values can be changed without rebuilding the Docker image

# Application name shown throughout the UI (login page, sidebar, dashboard)
PROJECT_NAME=MoMo Insights

# Logo - Supports both local paths and external URLs:
# - Local path: /momo.png (file must exist in /usr/share/nginx/html/)
# - External URL: https://upload.wikimedia.org/wikipedia/commons/2/28/Cricket_Wireless_%282014%29.svg
# - External URL: https://example.com/logo.png
PROJECT_LOGO=/momo.png

# Number of top APIs to show in API Analytics tab (default: 10)
# Increase this value to see more APIs in the top performers list
TOP_APIS_LIMIT=10

# ── Azure App Insights (REQUIRED) ──────────────────────────────────────────────
APP_INSIGHTS_APP_ID=your-app-insights-app-id-here
APP_INSIGHTS_API_KEY=your-app-insights-api-key-here

# ── Azure Infrastructure (REQUIRED) ────────────────────────────────────────────
AZURE_SUBSCRIPTION_ID=your-azure-subscription-id
AZURE_RESOURCE_GROUP=your-resource-group-name
AKS_CLUSTER_NAME=your-aks-cluster-name
MYSQL_SERVER_NAME=your-mysql-server-name

# ── Log Analytics (REQUIRED) ───────────────────────────────────────────────────
LOG_ANALYTICS_WORKSPACE_ID=your-log-analytics-workspace-id
LOG_ANALYTICS_AUTH_TOKEN=your-log-analytics-bearer-token

# ── Authentication (REQUIRED) ──────────────────────────────────────────────────
# Admin user — full access (create/edit/delete panels, dashboards, connections)
JWT_SECRET=your-strong-random-secret-minimum-32-characters
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-admin-password

# Viewer user — read-only access (view dashboards and panels, no modifications)
# Leave VIEWER_PASSWORD empty to disable the viewer account
VIEWER_USERNAME=viewer
VIEWER_PASSWORD=your-viewer-password

# Extra users — comma-separated list of username:password:role
# role must be either "admin" or "viewer"
# Example: EXTRA_USERS=alice:pass1:viewer,bob:pass2:admin,carol:pass3:viewer
EXTRA_USERS=

# ── PostgreSQL Custom DB (OPTIONAL) ────────────────────────────────────────────
# Query timeout in milliseconds (default: 30000 = 30 seconds)
# Increase for slow/complex queries
DB_QUERY_TIMEOUT=30000
# Max connections per pool (default: 5)
DB_MAX_POOL_SIZE=5

# ── Telegram Notifications (OPTIONAL) ──────────────────────────────────────────
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── WhatsApp Notifications (OPTIONAL) ──────────────────────────────────────────
WHATSAPP_API_URL=
WHATSAPP_API_KEY=

# ── Feature Flags (OPTIONAL) ───────────────────────────────────────────────────
ENABLE_ML_ALERTS=true
ENABLE_TELEGRAM=false
ENABLE_WHATSAPP=false
ENABLE_OUTAGE_DETECTION=true

# ═══════════════════════════════════════════════════════════════════════════════
# NOTES:
# 1. Never commit the actual .env.combined file with real credentials
# 2. Generate a strong JWT_SECRET: openssl rand -base64 32
# 3. Get Log Analytics token from Azure Portal or use Azure CLI
# 4. Enable features as needed by setting to true/false
# 5. Runtime customization (PROJECT_NAME, PROJECT_LOGO, TOP_APIS_LIMIT) can be
#    changed without rebuilding the Docker image - just restart the container
# 6. PROJECT_LOGO supports both local paths and external URLs:
#    - Local: PROJECT_LOGO="/logo.png" (add file to image or mount as volume)
#    - External: PROJECT_LOGO="https://example.com/logo.png" (no setup needed)
# ═══════════════════════════════════════════════════════════════════════════════
